version: v4
base: docker-registry.wikimedia.org/bullseye:latest

apt:
  packages:
    - prometheus
    - tox

variants:
  test:
    copies: [local]
    entrypoint: [tox]
    # Required so tox can write to the container
    runs:
      insecurely: True
